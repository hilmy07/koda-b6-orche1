# Node-Express-EJS-CRUD
This repo uses Node js, Express js, MongoDB, Mongoose and EJS to build a Blog App that utilises CRUD

Cara menjalankan aplikasi
- npm install
- tambahkan script "start": "node app.js" untuk menjalankan app di package.json
- perintah jalankan aplikasi npm start
